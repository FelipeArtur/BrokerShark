# ── Node / build ────────────────────────────────────────────────────────
node_modules/

# ── Segredos / ambiente ─────────────────────────────────────────────────
.env
.env.*
credentials/

# ── Dados locais (nunca versionar) ──────────────────────────────────────
# data/ = SQLite (ledger pessoal) — o ledger NUNCA é versionado.
# A doc de apoio saiu do repo (vive no vault Obsidian, ver README) — não há mais
# docs/ pra ignorar nem pra versionar.
data/
backups/
logs/
*.log

# ── Tooling de agentes (local, fora do VCS) ─────────────────────────────
.agents/
.gstack/
.antigravitycli/
.claude/settings.local.json

# ── IDE / editor ────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── OS ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

.superpowers/

# Tooling de agentes (local, fora do VCS)
.impeccable/
