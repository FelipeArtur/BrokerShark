# ── Node / build ────────────────────────────────────────────────────────
node_modules/
__pycache__/

# ── Segredos / ambiente ─────────────────────────────────────────────────
.env
.env.*
credentials/

# ── Dados locais (nunca versionar) ──────────────────────────────────────
# data/ = SQLite (ledger pessoal) — o ledger NUNCA é versionado.
# docs/ saiu daqui: specs/planos/auditoria não têm dado pessoal (verificado:
# nenhum valor em R$, nenhum número de conta) e valem como histórico de decisão.
data/
backups/
logs/
*.log

# ── Tooling de agentes (local, fora do VCS) ─────────────────────────────
.agents/
.gstack/
.antigravitycli/
.claude/settings.local.json

# ── IDE / editor ────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── OS ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

.superpowers/

# Tooling de agentes (local, fora do VCS)
.impeccable/
