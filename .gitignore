# ── Node / build ────────────────────────────────────────────────────────
node_modules/

# ── Segredos / ambiente ─────────────────────────────────────────────────
.env
.env.*
credentials/

# ── Dados locais (nunca versionar) ──────────────────────────────────────
# data/ = SQLite (ledger pessoal) — o ledger NUNCA é versionado.
# A doc de apoio saiu do repo (vive no vault Obsidian, ver README) — não há mais
# docs/ pra ignorar nem pra versionar.
data/
backups/

# config/local.json = as SUAS contas, bancos e keywords. Fica de fora do repo
# público de propósito: config/default.json é o exemplo genérico, e o que é seu
# não precisa virar vitrine. No repositório pessoal, versione com `git add -f`.
config/local.json
logs/
*.log

# ── Tooling de agentes (local, fora do VCS) ─────────────────────────────
.agents/
.gstack/
.antigravitycli/
.claude/settings.local.json

# ── IDE / editor ────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── OS ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

.superpowers/

# Tooling de agentes (local, fora do VCS)
.impeccable/
